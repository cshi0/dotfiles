- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.oh-my-zsh:
    
- shell:
    - chsh -s $(which zsh)
    - vim +PluginInstall +qall
    - python3 .vim/bundle/YouCompleteMe/install.py
